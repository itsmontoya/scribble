name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust-ci:
    name: fmt / clippy / test / docs
    runs-on: ubuntu-latest
    env:
      ALL_FEATURES: bin-scribble-cli,bin-model-downloader,bin-scribble-server

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust (stable) + components
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cargo cache
        uses: Swatinem/rust-cache@v2

      - name: Rustfmt (check)
        run: cargo fmt --all -- --check

      - name: Clippy (deny warnings)
        run: cargo clippy --locked --all-targets --features "$ALL_FEATURES" -- -D warnings

      - name: Tests
        run: cargo test --locked --features "$ALL_FEATURES"

      - name: Docs (no warnings as errors)
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --locked --no-deps --features "$ALL_FEATURES"

      - name: Publish (dry-run)
        if: >
          contains(github.event.pull_request.labels.*.name, 'patch') ||
          contains(github.event.pull_request.labels.*.name, 'minor') ||
          contains(github.event.pull_request.labels.*.name, 'major')
        run: cargo publish --locked --dry-run
